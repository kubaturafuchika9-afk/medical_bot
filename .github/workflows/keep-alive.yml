name: Keep Alive Bot

on:
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          # Замени medical-bot-xxxxx на свой URL
          curl -s https://medical-bot-xxxxx.onrender.com/health || true
